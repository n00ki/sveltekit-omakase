import type { AppConfig } from '../config.schema';

export const appConfig: AppConfig = {
  name: 'SvelteKit Omakase',
  description: 'the ultimate mise en place for your next SvelteKit project 🚀'
};
