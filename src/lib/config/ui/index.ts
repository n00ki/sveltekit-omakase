import type { UiConfig } from '../config.schema';

export const uiConfig: UiConfig = {
  // Delay before flash messages are cleared from the client (ms)
  flashClearAfterMs: 3000,
  // Mobile breakpoint used by responsive helpers (px)
  mobileBreakpoint: 768
};
