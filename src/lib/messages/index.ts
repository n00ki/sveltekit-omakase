import messages from './messages.json';

export const general = messages.general;
export const pages = messages.pages;
export const auth = messages.auth;
export const settings = messages.settings;

export default messages;
